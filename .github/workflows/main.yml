name: W11 Daily Tutor

on:
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install anthropic

      - name: Anleitungen generieren
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          cat > run.py << 'PYEOF'
          import anthropic, os, json, re
          from datetime import date
          from pathlib import Path

          client = anthropic.Anthropic()
          today = date.today()

          prompt = """Erstelle 2 Windows 11 Anleitungen auf Deutsch.
          Anleitung 1: W11 Basics. Anleitung 2: OneDrive.
          Antworte NUR mit diesem JSON ohne Backticks:
          {
            "anleitungen": [
              {
                "nr": 1,
                "titel": "Emoji Titel",
                "warum": "Nutzen in 1 Satz",
                "kategorie": "W11",
                "dauer": "5 Min",
                "schritte": [
                  {"nr": 1, "aktion": "Aktion", "erklaerung": "Erklaerung"},
                  {"nr": 2, "aktion": "Aktion", "erklaerung": "Erklaerung"},
                  {"nr": 3, "aktion": "Aktion", "erklaerung": "Erklaerung"}
                ],
                "quicktip": "Tipp",
                "w11_integration": "Hinweis",
                "faq_frage": "Frage?",
                "faq_antwort": "Antwort",
                "unterricht": "Aufgabe"
              },
              {
                "nr": 2,
                "titel": "Emoji Titel",
                "warum": "Nutzen in 1 Satz",
                "kategorie": "OneDrive",
                "dauer": "5 Min",
                "schritte": [
                  {"nr": 1, "aktion": "Aktion", "erklaerung": "Erklaerung"},
                  {"nr": 2, "aktion": "Aktion", "erklaerung": "Erklaerung"},
                  {"nr": 3, "aktion": "Aktion", "erklaerung": "Erklaerung"}
                ],
                "quicktip": "Tipp",
                "w11_integration": "Hinweis",
                "faq_frage": "Frage?",
                "faq_antwort": "Antwort",
                "unterri
